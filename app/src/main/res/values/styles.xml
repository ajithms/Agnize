<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:statusBarColor">#000000</item>
    </style>
    <style name="AppThemeCrop" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#3b7b9b</item>
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>
    <style name="AppThemeAlert" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">#3b7b9b</item>
        <item name="android:textColor">#000000</item>
    </style>

</resources>
